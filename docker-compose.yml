version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/dev.db
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - app-data:/app/data
    restart: always

volumes:
  app-data:
